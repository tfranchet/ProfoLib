{% extends 'base.html.twig' %}

{% block title %}Hello RdvController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    {% include '/header.html.twig' %}
    <div class="example-wrapper">
        <ul>
            <li>Le rendez-vous {{ rdv.id }} a bien été pris par {{ rdv.etudiant.name }}</li>
            <li>Il aura lieu de {{ rdv.heureDebut.timestamp }} à {{ rdv.heureFin.timestamp }}</li>
            <li><a href="{{ path('rdv_decommand', {'id':rdv.id}) }}"> decommander le rdv</a>
                        <a href="{{ path('rdv_take', {'id':rdv.etudiant.id}) }}"> Retourner a la liste</a>
                </li>
        </ul>
    </div>
{% endblock %}
